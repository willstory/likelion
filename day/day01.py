import sys


def push():
    if "push" in calc:
        calc.append()

    def pop():
        pop()

    def size():
        len(calc)
    
    def empty():
        if len(calc)>=1:
            print("1")
        if len(clac)==0:
            print("0")

    def front():
        if len(calc)<=1:
            print("-1")
        else:
            print(clkc[0])

    def back():
        if len(calc)<=1:
            print("-1")
        else:
            print(clac[-1])


order = input(sys.stdin.readline())

for calc in range(order):